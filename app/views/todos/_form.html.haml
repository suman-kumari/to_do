= form_for(@todo, :html => { :class => 'form-horizontal' }) do |f|  
  - if @todo.errors.any?
    #error_explanation.alert-danger
      %h2
        = pluralize(@todo.errors.count, "error")
        prohibited this list from being saved:
      %ul
        - @todo.errors.full_messages.each do |msg|
          %li= msg
  .control-group
    = f.label :title, class: "control-label"
    .controls
      = f.text_field :title, :placeholder => "Title"
  .control-group
    = f.label :description, class: "control-label"
    .controls
      = f.text_area :description, :rows => 3, :placeholder => "Description", class: "input-large" 
  .control-group
    = f.label :start_date_time, class: "control-label"
    .controls
      = f.text_field :start_date_time, class: "datepicker"
  .control-group
    = f.label :end_date_time, class: "control-label"
    .controls
      = f.text_field :end_date_time, class: "datepicker"
  .field
    = f.label :privacy
    = f.select(:privacy, [["public",1], ["private",0]])
  .field
    = f.label "Click task to be done"
  
    = f.check_box :completed
    %br/
  .actions
    = f.button :submit, :class => "btn btn-primary"








